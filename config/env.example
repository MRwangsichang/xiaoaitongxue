# ==================== 智能家居助手 环境变量示例 ====================
# 用途: 配置敏感信息和系统参数
# 使用方法: 
#   1. 复制此文件为 .env.local
#   2. 填入真实的密钥和配置
#   3. 确保 .env.local 在 .gitignore 中（不要提交到仓库）

# ==================== Grok AI 配置 ====================
# 用于Rules模块的对话兜底和Wakeword/Greeting服务的模板选择
GROK_API_KEY=your_grok_api_key_here
GROK_API_BASE=https://api.x.ai/v1
GROK_MODEL=grok-beta

# ==================== 讯飞语音识别 (ASR) ====================
# 实时流式语音识别
XUNFEI_APP_ID=your_xunfei_app_id
XUNFEI_API_KEY=your_xunfei_api_key
XUNFEI_API_SECRET=your_xunfei_api_secret

# ==================== 讯飞语音合成 (TTS) ====================
# x5超拟人语音合成
XUNFEI_TTS_APP_ID=your_xunfei_tts_app_id
XUNFEI_TTS_API_KEY=your_xunfei_tts_api_key
XUNFEI_TTS_API_SECRET=your_xunfei_tts_api_secret

# ==================== MQTT 配置 ====================
# 消息总线（默认本地）
MQTT_BROKER=localhost
MQTT_PORT=1883
MQTT_USERNAME=
MQTT_PASSWORD=
MQTT_KEEPALIVE=60

# ==================== 网络代理配置 ====================
# 用于访问Grok API（如果需要）
# 闪连VPN或其他SOCKS5代理
SOCKS5_PROXY=127.0.0.1:1080
HTTP_PROXY=
HTTPS_PROXY=

# ==================== 音频设备配置 ====================
# 麦克风设备（ASR）
AUDIO_INPUT_DEVICE=plughw:1,0

# 音箱设备（TTS）
AUDIO_OUTPUT_DEVICE=plughw:0,0

# ==================== 摄像头配置 ====================
# 视觉扫描使用的摄像头
CAMERA_DEVICE=/dev/video0
CAMERA_RESOLUTION=640x480
CAMERA_FRAMERATE=10

# ==================== 日志配置 ====================
# 日志级别: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# 日志目录
LOG_DIR=logs

# 日志轮转天数
LOG_ROTATE_DAYS=7

# ==================== 系统配置 ====================
# 项目根目录（绝对路径）
PROJECT_ROOT=/home/MRwang/smart_assistant

# 服务前缀（MQTT主题）
SERVICE_PREFIX=sa

# ==================== 人脸识别配置 ====================
# 识别模型路径
FACE_MODEL_PATH=data/faces/王总_v2.yml

# 识别阈值（越低越严格）
FACE_CONFIDENCE_THRESHOLD=60

# ==================== 记忆系统配置（待实现）====================
# 数据库路径
MEMORY_DB_PATH=data/memory/conversations.db

# 最大保存对话数
MEMORY_MAX_RECORDS=1000

# 自动清理旧数据（天）
MEMORY_RETENTION_DAYS=30

# ==================== 开发配置 ====================
# 开发模式（启用更详细日志）
DEBUG_MODE=false

# 模拟模式（使用mock provider）
MOCK_ASR=false
MOCK_TTS=false

# ==================== 备注 ====================
# - 所有以 *_KEY、*_SECRET 结尾的变量都是敏感信息
# - 不要将真实配置提交到Git仓库
# - 部署到新环境时，先复制此文件为 .env.local，再填入真实值
# - 如果某个变量为空，表示使用默认值或该功能未启用

# ==================== 示例值说明 ====================
# GROK_API_KEY: 从 https://console.x.ai/ 获取
# XUNFEI_*: 从 https://console.xfyun.cn/ 获取
# AUDIO_*_DEVICE: 运行 arecord -l 和 aplay -l 查看设备名
# CAMERA_DEVICE: 运行 ls -l /dev/video* 查看设备路径
